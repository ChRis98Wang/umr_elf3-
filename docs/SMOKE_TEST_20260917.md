# 独立仓库完整样本实跑

日期：2026-09-17，Asia/Shanghai。目的：验证 README 的本地流程真正能够执行，
不是只检查 `--help` 或仅运行单元测试。原来的 9,483 条全库队列没有恢复。

## 执行范围

- 仓库基础提交：`fe041d98ecb844cc088748e82f05141e919363f8`。
- 从原库选择一条短的、此前已进入待复核的完整动作，191 帧，50 Hz，时间跨度 3.80 秒。
- 使用既有 Python 环境、已校验的 ELF3 资产和关节契约，没有重新安装 IsaacLab。
- 重新执行 URDF 转换、单源清单、批计划、启动器和完整 worker。
- 准备完整 SMPL-X 表面；在一个空的新缓存中重新进行 Stage I 训练
  （4,096 点、2,500 epoch、CUDA），没有把旧对应结果直接复制过来。
- 连续 Stage II IK，然后全身 B 样条优化。最后单独运行 README 的 `preview` 命令。

这个样本是为检查软件执行路径选取的，不是随机抽样，不能从单条成功推断全库质量。
它也不是新增动作，不计入 487 条之外的新样本。

## 实测结果

| 项目 | 结果 |
|---|---:|
| 完整输出帧数 | 191 |
| 计算队列耗时（不含随后录像） | 139.44 s |
| Stage I epoch | 2,500 |
| Stage I 学习耗时（日志记录） | 18.01 s |
| Stage II / warmup 求解失败 | 0 / 0 |
| 全身优化迭代 | 262 |
| 全身优化耗时 | 101.88 s |
| 优化正常收敛 | 是 |
| 关节角度 / 速度超限 | 0 / 0 |
| 最低脚部网格高度 | +1.986 mm |
| 原始碰撞几何最大自穿透 | 0 mm |
| 当前手臂—躯干凸包筛查最大穿透 | 0 mm |
| 末端相对原始重定向位移 P95 | 1.660 mm |
| 末端相对原始重定向最大位移 | 7.152 mm |
| 最终状态 | `kinematic_review_required` |
| 正式批准训练 / 物理跟踪验证 | 否 / 否 |

这里只报告当前碰撞筛查范围，不能宣称整个机器人绝对无碰撞。

## 本地回放

`run_elf3_umr_trial.py preview` 成功产生完整 191 帧 MP4 和下采样 GIF，
并检查了一张中间帧。画面明确标注 `KINEMATIC ONLY` 和非策略、非物体操作。
录像通过直接设置 `qpos` 和正向运动学渲染，不执行 `mj_step`。

这些录像与输入、轨迹保留在被 Git 忽略的本地目录，没有公开发布。

## 退出与可复现性

验证服务自然结束后，`MainPID=0`、`ActiveState=inactive`、控制组为空；
回放导出完成后也未留下相关 worker/render 进程。原全库服务保持停止。

计算配方未变；新的模型 XML、准备文件与队列有自己的哈希和输出目录。
正式复现需合法持有同一源数据与模型，并保留数据划分；首条冒烟测试本身不用于
训练/验证指标对比。公开单元测试则不需要这些私有数据。
